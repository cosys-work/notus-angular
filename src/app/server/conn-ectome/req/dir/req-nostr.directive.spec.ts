import { ReqNostrDirective } from './req-nostr.directive';

describe('ReqNostrDirective', () => {
  it('should create an instance', () => {
    const directive = new ReqNostrDirective();
    expect(directive).toBeTruthy();
  });
});
