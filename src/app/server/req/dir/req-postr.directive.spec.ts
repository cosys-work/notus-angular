import { ReqPostrDirective } from './req-postr.directive';

describe('ReqPostrDirective', () => {
  it('should create an instance', () => {
    const directive = new ReqPostrDirective();
    expect(directive).toBeTruthy();
  });
});
